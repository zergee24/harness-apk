# Harness APK 领域词汇

本文件统一 Harness APK 中与项目工作、移动捕获和可回放工作简报有关的产品语言。它只定义领域对象，不描述实现方式。

## 工作归属

**项目（Project）**：
长期工作事实、交付物和版本历史的唯一归属。会话、运行和工作简报可以归属项目，但不能取代项目。
_Avoid_: 工作区、Notebook、知识库

**运行（Run）**：
围绕一个明确目标发生的一次本地生成或远程 Agent 执行。运行产生结构化过程和结果证据，不等同于用户的工作记录。
_Avoid_: 任务线程、会话、录制

**项目事实（Project Fact）**：
已经由用户确认的项目 Markdown，或由文件、测试、Git 等结构化证据证明的当前事实。用户口述、手写内容和 AI 总结在确认前都不是项目事实。
_Avoid_: 记忆、笔记、AI 结论

## 可回放工作简报

**可回放工作简报（Replayable Work Brief）**：
归属于一个项目、用于恢复某次工作判断的复合交付物。它把用户主动记录的画布、录音转写、标记和代码证据按时间组织，但不承诺复现全部屏幕或系统操作。
_Avoid_: 白板文件、会议录像、工作流画布、完整录屏

**记录场次（Capture Session）**：
用户明确开始并结束的一段工作捕获过程。v1 中一份可回放工作简报只允许一个记录场次；暂停、恢复和崩溃续录形成捕获片段，不形成新的记录场次。
_Avoid_: Run、会话、录音文件

**Run 证据关联（Brief Run Evidence Link）**：
把工作简报与同一项目中的 Run 不可变证据快照显式关联起来的引用。Run 仍归属于项目，不归属于简报；没有 Run 的需求或架构简报也可以独立成立。
_Avoid_: 自动绑定、实时 Run 镜像、Run 归属

**画布页（Canvas Page）**：
记录场次中承载笔迹、底图和空间关系的一张有限页面。画布页不是流程编排节点，也不是独立交付物。
_Avoid_: 无限画布、Workflow、白板项目

**时间锚点（Timeline Anchor）**：
把某个用户标记、笔迹状态、转写片段或代码证据定位到记录场次具体时刻的引用。时间锚点表达“何时相关”，不自动证明内容正确。
_Avoid_: 系统时间、日志时间戳、项目事实

**用户标记（User Marker）**：
用户主动标出的决策、问题、待办或书签。用户标记比自动转写和 AI 视觉推断更接近用户意图，但仍需经过项目沉淀流程才能成为项目事实。
_Avoid_: AI 标签、自动结论

**代码锚点（Code Anchor）**：
从简报定位到项目文件、Diff、Commit、测试证据或 Run 证据的可追溯引用。它保留记录时的版本或内容指纹，不能静默漂移到当前代码。
_Avoid_: 代码副本、绝对路径、当前文件链接

**纪要候选（Brief Summary Candidate）**：
根据用户选择的简报证据生成、等待用户审核的结构化总结。它只有通过现有项目 Diff 与 Apply 流程后才能成为项目事实。
_Avoid_: 自动纪要、项目记忆、已确认结论
